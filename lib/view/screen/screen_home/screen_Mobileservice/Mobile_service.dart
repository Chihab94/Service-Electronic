import 'package:flutter/material.dart';

class Mobile_service extends StatelessWidget {
  const Mobile_service({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
